# ループ状態

このファイルはループの唯一の進捗の正。セッションの記憶より優先する。
手で編集してもよい。

最終実行: 2026-08-18（#167）

## 対応中

一度に1件だけ。PR を作ったら「完了」へ移す。

| issue | 着手日 | ブランチ | 状況 |
|---|---|---|---|
| - | - | - | - |

## 完了（直近5件）

| issue | 完了日 | PR |
|---|---|---|
| #167 | 2026-08-18 | #182 |
| #168 | 2026-08-18 | #181 |
| #164 | 2026-08-18 | #180 |
| #165 | 2026-08-17 | #179 |
| #166 | 2026-08-16 | #178 |

## 人間待ち（blocked）

ラベル未設定の issue は「人間待ち」に含めない。ラベルが付いていないこと自体は待ち状態ではない。

| issue | 待っている判断 | 滞留開始 |
|---|---|---|
| - | - | - |

（#130 は 2026-08-09 時点でクローズ済みのため削除した）

## 試行回数

3回に達したら停止し、draft PR を出して `blocked` を提案する。

| issue | 試行回数 | 最後に落ちたゲート |
|---|---|---|
| - | - | - |

## ラベル提案済み

一度提案した issue には再投稿しない。1実行あたりの投稿上限は3件。

| issue | 提案したラベル | 提案日 |
|---|---|---|
| #126 | human | 2026-08-06 |
| #129 | blocked | 2026-08-06 |
| #130 | blocked | 2026-08-06 |
| #143 | agent:pr | 2026-08-11 |
| #134 | human | 2026-08-11 |
| #135 | human | 2026-08-11 |
| #139 | blocked | 2026-08-11 |
| #155 | agent:pr | 2026-08-11 |
| #159 | human | 2026-08-18 |
| #187 | human | 2026-08-18 |

## 次回への申し送り

**契約の欠陥で止まった件だけ**をここに記録する。溜まったものが
`.github/ISSUE_TEMPLATE/agent-task.yml` の改善点になる。

- 2026-08-06 #129: **契約が state 更新を禁止していて着手できなかった。**
  `.claude/**` を「触ってはいけないファイル」に入れ、かつ完了条件で差分ファイル数を
  固定すると、ループ自身の記帳（`.claude/state/loop.md`）と必ず衝突する。
  **この issue に固有の問題ではなく、設計上の穴だった。**
  → **対応済み。** state は git 追跡下に置いたまま、どの契約でも更新してよい例外とした。
    Routines / クラウドセッションは fresh clone で動くため、追跡外にすると
    毎回空の状態から始まってしまう。
    プレフライト検査に「差分ファイル数を固定していないか」「契約が state 更新を
    禁止していないか」の2項目を追加した。
- 2026-08-09 #136: **プレフライト検査は通ったが、実装して初めて契約の矛盾が判明した。**
  完了条件が既存 request spec 4件を「無修正で通る」と固定していたが、それらは
  `allow(TokenIssuer).to receive(:decode).with("valid_token")` で stub しており、
  RSpec の verifying partial double が**実装の引数シグネチャを検証する**ため、
  `decode` に必須キーワード引数を足した時点で stub の定義自体が
  `Missing required keyword arguments: purpose` で落ちる。呼び出し側の書き換えとは無関係。
  → **プレフライト検査に追加すべき項目**: 「完了条件が『既存 spec を無修正で通す』と
    書いているとき、その spec が変更対象のメソッドを stub していないか」。
    stub していれば、シグネチャ変更と無修正通過は原理的に両立しない。
  さらに**2つ目の欠陥**: `refresh_tokens.token` が `varchar(128)` で、`purpose` クレームを
  足すと JWT が上限を超え `PG::StringDataRightTruncation` になる（9件失敗）。
  カラム拡張には新規マイグレーションが要るが、`db/migrate/**` は「触ってはいけない
  ファイル」で、新規追加も「触ってよいファイル」に無い。
  → **起票時にペイロードを増やす変更は、格納先カラムの桁数を確認する**必要がある。
  → **人間の判断で契約を広げて解決した（選択肢 A）。** 既存 spec 4件の stub 更新と
    `db/migrate/` の新規追加を許可してもらい、5ゲート合格まで到達して PR #141 を ready にした。
- 2026-08-09 #138: **契約が「触ってよい」としたファイルを、強制層（C層）が触らせなかった。**
  完了条件に `.env.example` への環境変数6件の追加があり、「触ってよいファイル」にも入っていたが、
  `.claude/settings.json` の `deny` にある `Read(/.env*)` が `.env.example` にも一致するため、
  Read ツール・`cat`・追記の3通りすべてが拒否された。Edit ツールは事前 Read を要求するので
  deny を回避できない。**実装の失敗ではなく、契約と強制層の衝突。**
  → **プレフライト検査に追加すべき項目**: 「『触ってよいファイル』が `settings.json` の
    `deny` パターンに一致していないか」。とくに `.env.example` は
    `Read(/.env*)` に**意図せず巻き込まれる**（秘密情報の保護が目的のルールなのに、
    公開テンプレートまで止まる）。
  → **人間が deny の範囲を絞って解決した。** `Read(/.env*)` を個別列挙に置き換えた結果
    `.env.example` を編集でき、全完了条件を満たして PR #147 を ready にした。
    プレフライト検査への追加は未対応（次に触るときにやる）。
- 2026-08-10 #145: **完了条件が「触ってよいファイル」の外にあるファイルの変更を要求していた。**
  プレフライト検査は通り、実装も完走したが、契約を厳密に守ると満たせない完了条件が2つあった。
  ①`git grep -i clerk -- app lib config spec` を空にするには `lib/tasks/dev.rake`
  （Clerk 用の開発 JWT 発行タスク。消費者の `ClerkClient` ごと消えるので死にコード）の
  削除が必要だが、`lib/` の許可は `lib/token_issuer.rb` のみだった。
  ②`ClerkClient::UnauthorizedError` を `UnauthorizedError` に置き換えるには
  `app/errors/unauthorized_error.rb` の新規作成が必要だが、`app/errors/**` は許可にも
  禁止にも入っていなかった。**どちらも「触ってはいけないファイル」ではない**ため、
  issue の「ここに書かれていない迷いは自分で判断して進んでよい」に従って実施し、PR に明記した。
  → **起票時のチェック項目**: 「完了条件の grep 対象ディレクトリが、そのまま
    『触ってよいファイル』に含まれているか」。`git grep -- <dir>` を空にする条件は、
    その `<dir>` 配下すべての編集許可と等価である。
  さらに**3つ目**: `docs/development/directory.md` が Clerk 前提のまま取り残された。
  実装は `docs/` を単一の情報源として読む（`loop-policy.md` §7）のに、その `docs/` の更新が
  禁止されていたため、**次周のループが削除済みクラスを前提に実装してしまう**状態だった。
  → **人間の判断で契約を広げて解決した。** `docs/development/directory.md` の更新許可をもらい、
    controllers / services / models / lib のツリーを現状に更新して PR #149 に含めた。
  → **起票時のチェック項目**: 「レイヤー構成を変える issue で、`docs/development/directory.md`
    が『触ってはいけないファイル』に入っていないか」。構成変更と directory.md の凍結は両立しない。
- 2026-08-10 #146: **完了条件どうしが両立しなかった（grep の除外リストに新規 ADR が入っていない）。**
  完了条件①は `git grep -ril clerk -- docs README.md` の出力を
  `docs/decisions/002` `007` `008` の**3ファイルだけ**にすることを求めていたが、
  完了条件⑤は `docs/decisions/009-authentication.md` に
  「なぜ Clerk をやめたか」という見出しを含めることを求めている。
  **旧基盤の名前を含む ADR を新規に作れと言いながら、その名前の grep を空にしろと言っている。**
  → 到達できたのは「3ファイル + 009」。009 は歴史的記録として 002/007/008 と同じ扱いにすべきもの。
  → **起票時のチェック項目**: 「grep を空にする完了条件の除外リストに、
    **同じ issue で新規作成するファイル**が入っているか」。
    ADR は旧構成の名前を必ず含むため、移行 issue では毎回これが起きる。
  もう1点、`.env.example` の扱い：完了条件②（開発用 JWT シークレットの環境変数を
  リポジトリ全体から消す）を満たすには `.env.example` の編集が必要だが、
  「触ってよいファイル」に無い。**「触ってはいけないファイル」にも無い**ため
  #145 と同じ判断で実施し、PR に明記した（3周連続で同じ形の欠陥）。
- 2026-08-11 #139: **完了条件が要求するコマンドが、CI に存在しない前提（DB 接続）を必要とした。**
  完了条件①は `type_check` ジョブで `bundle exec rake rbs_rails:all` を `steep check` の前に
  実行することを求めているが、`rbs_rails all` は Rails 環境をロードして `klass.columns` を
  引くため**DB 接続が必須**。到達不能な `DATABASE_URL` を与えて実行し
  `There is an issue connecting with your hostname` で落ちることを確認した。
  CI の `type_check` ジョブには postgres service も DB 系 env も無い（`test` ジョブにしか無い）。
  一方で完了条件②と「触ってはいけないファイル」が `.gitignore` の `sig/rbs_rails/` 行を凍結して
  いるため、**生成物をコミットして回避する道も塞がれている**。
  → issue 記載のエスカレーション条件「`rake rbs_rails:all` が CI 環境で失敗し、DB 接続など
    追加のセットアップが必要になった場合は停止する」に該当し、draft PR で停止した。
  → **人間の判断で契約を広げて解決した（選択肢 A）。** `type_check` ジョブに postgres service と
    DB 系 env、`rails db:prepare` の追加を許可してもらった（issue の「触ってよいファイル」は
    `ci.yml` を「ステップ追加のみ」に限っていたため、service / env ブロックの追加は契約の拡大）。
    代案の「`db/schema_cache.yml` をコミットして DB なしで生成する」は**成立しないことを実証した**。
    `rbs_rails` は `active_record.rb:8` で `klass.connection.table_exists?` を無条件に呼ぶため、
    schema cache があっても実接続を要求する（到達不能な DB で exit 1 を確認）。
    **CI 実機で4ジョブすべて緑を確認して PR #153 を ready にした**（type_check 1m9s、
    ローカルと同じ51ファイル検査で `No type error detected`）。
  → **今回得た教訓**: 「ローカルの5ゲート合格」は「CI 緑」を意味しない。ローカルの docker には
    DB があるため、CI にしか無い前提の欠落は `gate-verifier` では原理的に検出できない。
    **CI の実行環境を変える issue では、push して実機の CI が緑になるまで完了としない。**
  → **起票時のチェック項目**: 「CI に追加するコマンドが、そのジョブに存在しない
    サービス（DB・Redis 等）を必要としないか」。`rake` タスクは `:environment` 依存だと
    暗黙に DB を要求する。**ローカルの docker には DB があるため `gate-verifier` は通ってしまい、
    この欠陥は機械判定ゲートでは検出できない**（5ゲート合格と CI 緑が一致しない唯一の型）。
  もう1点、`sig/app/constants/**`：`models` ターゲットを `D::Ruby.default` にすると
  `UserConstants` / `UserIdentityConstants` が `UnknownConstant` になるため sig が要るが、
  「触ってよいファイル」は `sig/app/models/**` までだった。**「触ってはいけないファイル」にも
  無い**ため #145 / #146 と同じ判断で実施し、PR に明記した（4周連続で同じ形の欠陥）。
  → **起票時のチェック項目**: 「型検査の対象を広げる issue では、対象コードが**参照する側**
    （定数・他レイヤー）の sig も『触ってよいファイル』に含める」。
- 2026-08-11 運用: **1周のコメント上限3件を1件超過した（計4件）。**
  トリアージでラベル未設定の #143 / #134 / #135 に3件投稿して上限を使い切った後、
  #139 が停止条件に該当し、`implement-issue` Step 11 が要求する `blocked` 提案コメントを
  出す枠が残っていなかった。**エスカレーションのコメントは省略できない**ため投稿した。
  → `loop-policy.md` §6 の上限は「ラベル提案の一斉投稿」を抑えるためのものなので、
    **停止条件によるエスカレーションのコメントを枠外にする**か、
    トリアージ側の投稿上限を2件にして1枠を確保するか、どちらかに整理したい。
    現状の文言だとこの衝突が毎回起きる。
- 2026-08-17 #165: **grep を空にする完了条件の対象が「触ってよいファイル」からはみ出した（5周連続で同じ形）。**
  完了条件「リポジトリ全体を `shelfie.com` で grep してヒットが0件」を満たすには
  `docs/api/users/create.md:55`（Set-Cookie 例の `Domain=shelfie.com`）の修正が必要だが、
  「触ってよいファイル」は `docs/development/**` までで `docs/api/**` は入っていない。
  **「触ってはいけないファイル」にも無い**ため #145 / #146 / #139 と同じ判断で実施し、PR に明記した。
  → **起票時のチェック項目**（既出だが未反映）: 「`git grep` をリポジトリ全体に掛ける完了条件は、
    **リポジトリ全体の編集許可と等価**である」。対象を絞れないなら、事前に grep して
    ヒットするファイルをすべて「触ってよいファイル」に列挙する。
  もう1点、**`guideline-reviewer` の 🔴 が契約の穴を埋めた**: `README.md:17` の
  `| デプロイ | Kamal |` は issue の「やること」に無かったが、Kamal 設定を消す PR 自身と
  矛盾するため 🔴 になった。README は「触ってよいファイル」だったので修正できた。
  → **起票時のチェック項目**: 「何かを撤去する issue では、その名前を grep して
    **説明文・技術スタック表の記述**も『やること』に含める」。設定ファイルの削除だけを
    列挙すると、ドキュメント側が撤去前の状態で取り残される。
- 2026-08-18 #164: **「触ってよいファイル」が同種の設定を1つの環境ファイルにしか許していなかった（6周連続で同じ形）。**
  「やること」は `config/environments/production.rb` の `config.active_storage.service` 削除だけを挙げ、
  「触ってよいファイル」も production.rb までだったが、**同じ行が development.rb と test.rb にもあり**、
  `active_storage/engine` の require を外すと `undefined key 'active_storage'` ではなく
  `NoMethodError: undefined method 'active_storage'` で boot 不能になる（rspec 25件が全滅）。
  Rails の config は未ロードの railtie 名を OrderedOptions で吸収しない。
  **「触ってはいけないファイル」にも無い**ため #145 / #146 / #139 / #165 と同じ判断で実施し、PR に明記した。
  → **起票時のチェック項目**: 「`config.<framework>.*` を消す issue では、`config/environments/*.rb`
    **3本すべて**を grep して『触ってよいファイル』に入れる」。環境ファイルは雛形が同じ行を持つため、
    1本だけ許可すると必ず boot が壊れる。
  → 同じく **binstub と Gemfile はセット**: `gem "kamal"` を消すと `bin/kamal` が
    実行時に必ず `Gem::Exception` で落ちるファイルとして残る。gem 削除の issue では
    `bin/<gem 名>` を「触ってよいファイル」に含める。
  もう1点、**契約が撤去の範囲を明示していたおかげで止まれた**: `guideline-reviewer` は
  `action_mailer` / `active_job` の require も 🔴 で撤去を求めたが、issue が削除対象を
  「4本の require」と明示していたため範囲を守って見送れた。**列挙は過剰撤去を防ぐ**。
- 2026-08-18 #168: **完了条件の grep 対象に、実装コードの「コメント」が含まれることを見落としやすい。**
  完了条件「`lib/` を `secret_key_base` で grep してヒットが0件」に対し、**なぜ分離したかを
  説明するコメント**（「secret_key_base とは分ける」）がそのままヒットする。
  実装としては正しいのに完了条件が落ちるため、コメントを言い換えて回避した。
  → **起票時のチェック項目**: 「撤去した名前を grep で0件にする完了条件は、その名前が
    **why コメントに出てくることを許すのか**を明記する」。コメント規約が why を書けと
    求めている以上、撤去の理由を書くと撤去した名前が必ず登場する。
  もう1点、**契約が同種の設定を1ファイルにしか許していなかった**（#164 と同型）:
  `docs/development/secrets.md` は「触ってよいファイル」に無いが、
  「**不要な変数** | JWT 署名鍵の専用変数」と書いており PR 自身と矛盾し、さらに投入手順に
  `JWT_SECRET_KEY` が無いままだと**手順どおりに本番を構築すると起動が KeyError で失敗する**。
  「触ってはいけないファイル」にも無いため #145 / #146 / #165 と同じ判断で実施した。
  → **起票時のチェック項目**: 「環境変数を1つ増やす issue では、`.env.example` / `README` /
    `ci.yml` だけでなく **`docs/development/secrets.md` の投入手順と個数**も『触ってよいファイル』に含める」。
  → **設計上の学び**: `ENV.fetch(key, default)` は**空文字を弾かない**。`env_file` 経由だと
    キー自体は定義されるため、`.env.example` に `KEY=` と書いた状態で `cp` すると
    「設定したのに空の鍵で署名する」が起きる。`ENV[key].presence || ...` を使う。
    これは `guideline-reviewer` の 🟡 で見つかった（5ゲートでは検出できない型）。
- 2026-08-18 #167: **5ゲートが1行も検査しない成果物（ワークフロー YAML）の issue だった。**
  変更は `.github/workflows/**` と `docs/**` のみで Ruby コードが無いため、
  **`gate-verifier` の合格は成果物の妥当性を何も保証しない**。完了条件が
  「`actionlint` などで YAML の構文が妥当である」と機械判定を1つ用意していたのが効いた。
  加えて、SSM に送るリモートスクリプトを実際に生成して `bash -n` と `shellcheck` に掛けた。
  → **起票時のチェック項目**: 「成果物が Ruby でない issue では、**その成果物を検査する
    コマンド**を完了条件に必ず1つ入れる」。5ゲートは通って当然なので判断材料にならない。
  → **`guideline-reviewer` が実行時の破綻を3件見つけた**（5ゲートでは原理的に検出できない型）:
    ①`AWS-RunShellScript` は `/bin/sh`（Ubuntu では dash）で実行されるため、shebang 無しの
    `set -euo pipefail` は**1行目で全滅**する。②`aws ssm wait command-executed` の上限は
    5秒×20回=**100秒**で、`docker pull` + Rails フルブートに足りない。③`cmd | grep -c ... || true`
    は**コマンド自体の失敗を「0件」に潰す**。
    → **issue にコード例が載っていても、そのまま写して終わりにしない。** 今回は3つとも
      issue 本文のスクリプト例に含まれていた欠陥だった。
  もう1点、**直前の issue の成果物が次の issue の契約に反映されていない**: #168 で
  `JWT_SECRET_KEY` を必須にしたが、#167 の必須キー検証リスト（issue 本文に列挙）には無い。
  無いままだと「デプロイは緑なのにアプリが起動しない」になるため追加して PR に明記した。
  → **起票時のチェック項目**: 「先行 issue が環境変数を増やすとき、後続 issue の
    **ハードコードされたリスト**にも反映されるかを確認する」。issue 本文のコード例は
    起票時点のスナップショットなので、順序が入れ替わると必ず腐る。
- 2026-08-06 #130: プレフライト検査で2項目不合格、着手せず差し戻した。
  ①完了条件が機械判定不能（意図的に仕込んだもの。検査は正しく検出した）
  ②`.claude/**` の一括禁止（**仕込んでいない欠陥**。#131 で追加した検査項目が拾った）
  → `.claude/**` を一括で禁止する書き方は繰り返し出る。テンプレートの警告だけでは
    手書きの issue に効かないので、`/agent-issue` を使わせる導線を強めるか検討する。
